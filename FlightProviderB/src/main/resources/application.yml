server:
    port: 8082

spring:
    application:
        name: flight-provider-b

management:
    endpoints:
        web:
            exposure:
                include: health,info
    health:
        redis:
            enabled: false
                
